/*

Node.js is a JavaScript interpreter based on the V8 JavaScript engine
It is currently the most popular server-side JS interpreter

To run a JavaScript file with node, we use 'node /path/to/file.js' in the
  command-line, you may need to navigate to the folder first
To change "directory" in the command-line, use 'cd x', where x is the
  subdirectory

*/